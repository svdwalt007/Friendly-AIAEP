# audit-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build audit-service` to build the library.

## Running unit tests

Run `nx test audit-service` to execute the unit tests via [Vitest](https://vitest.dev/).
