# project-registry

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build project-registry` to build the library.

## Running unit tests

Run `nx test project-registry` to execute the unit tests via [Vitest](https://vitest.dev/).
