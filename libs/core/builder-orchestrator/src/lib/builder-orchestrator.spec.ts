import { builderOrchestrator } from './builder-orchestrator';

describe('builderOrchestrator', () => {
  it('should work', () => {
    expect(builderOrchestrator()).toEqual('builder-orchestrator');
  });
});
