# mock-api-server

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build mock-api-server` to build the library.

## Running unit tests

Run `nx test mock-api-server` to execute the unit tests via [Vitest](https://vitest.dev/).
