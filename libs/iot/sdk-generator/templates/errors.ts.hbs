// Generated by @friendly-tech/iot/sdk-generator

export interface FriendlyApiErrorDetails {
  statusCode: number;
  message: string;
  requestId?: string;
  apiSource: string;
}

export class FriendlyApiError extends Error {
  public readonly statusCode: number;
  public readonly requestId?: string;
  public readonly apiSource: string;

  constructor(details: FriendlyApiErrorDetails) {
    super(details.message);
    this.name = 'FriendlyApiError';
    this.statusCode = details.statusCode;
    this.requestId = details.requestId;
    this.apiSource = details.apiSource;

    // Maintains proper stack trace for where our error was thrown (only available on V8)
    if (Error.captureStackTrace) {
      Error.captureStackTrace(this, FriendlyApiError);
    }
  }

  toJSON() {
    return {
      name: this.name,
      message: this.message,
      statusCode: this.statusCode,
      requestId: this.requestId,
      apiSource: this.apiSource,
    };
  }
}
