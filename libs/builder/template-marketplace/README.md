# template-marketplace

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build template-marketplace` to build the library.

## Running unit tests

Run `nx test template-marketplace` to execute the unit tests via [Vitest](https://vitest.dev/).
