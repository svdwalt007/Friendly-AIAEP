import { templateMarketplace } from './template-marketplace';

describe('templateMarketplace', () => {
  it('should work', () => {
    expect(templateMarketplace()).toEqual('template-marketplace');
  });
});
