# publish-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build publish-service` to build the library.

## Running unit tests

Run `nx test publish-service` to execute the unit tests via [Vitest](https://vitest.dev/).
