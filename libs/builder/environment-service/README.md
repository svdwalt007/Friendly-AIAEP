# environment-service

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build environment-service` to build the library.

## Running unit tests

Run `nx test environment-service` to execute the unit tests via [Vitest](https://vitest.dev/).
