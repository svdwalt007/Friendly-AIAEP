# dashboard-templates

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dashboard-templates` to build the library.

## Running unit tests

Run `nx test dashboard-templates` to execute the unit tests via [Vitest](https://vitest.dev/).
