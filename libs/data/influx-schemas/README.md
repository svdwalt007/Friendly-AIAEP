# influx-schemas

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build influx-schemas` to build the library.

## Running unit tests

Run `nx test influx-schemas` to execute the unit tests via [Vitest](https://vitest.dev/).
