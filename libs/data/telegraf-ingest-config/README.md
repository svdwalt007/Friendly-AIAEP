# telegraf-ingest-config

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build telegraf-ingest-config` to build the library.

## Running unit tests

Run `nx test telegraf-ingest-config` to execute the unit tests via [Vitest](https://vitest.dev/).
